<Page x:Class="School.Pages.RaspisanieMaker"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:School.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Составление расписания">

    <Grid>
        <DataGrid HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Урок" Binding="{Binding Path=Teacher.FIO}" Width="*"/>
                <DataGridTextColumn Header="Понедельник" Binding="{Binding Path=Curse.EndDate}" Width="*">
                    
                </DataGridTextColumn>
                <DataGridTextColumn Header="Вторник" Binding="{Binding Path=Curse.EndDate}" Width="*"/>
                <DataGridTextColumn Header="Среда" Binding="{Binding Path=Curse.EndDate}" Width="*"/>
                <DataGridTextColumn Header="Четверг" Binding="{Binding Path=Curse.EndDate}" Width="*"/>
                <DataGridTextColumn Header="Пятница" Binding="{Binding Path=Curse.EndDate}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
